// Logical operators are used  for comparing two expressions
#include <bits/stdc++.h>
#include <conio.h>

using namespace std;
int main() {
    int a=45,b=89;
    cout<<"The Value of this logical operator ((a==b) && (a<b)) is "<<endl<<((a==b) && (a<b))<<endl;
        getch();
    return 0;
}