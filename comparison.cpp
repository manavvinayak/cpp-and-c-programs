// A brief into comparison operators 
// comparsion operators are basically those operators that compares two values
#include <bits/stdc++.h>
// this header file is a shortcut for including all header files (like mathematical operations) into its domain
#include <conio.h>
// This Header File is used for showing the screen of application software of a particular c++ file
using namespace std;
// used for printing the text
int main() {
    int a=45,b=67;
  
    cout<<"the chances of  value of a<b is "<<(a<b)<<endl;
    // The Function a<b will compare a and b values and check if b is greater than a , the output will be one if greater and 0 if not!
        getch();
        // getch function is used with conio header file
        // to show the output in the application software screen of particular  c++ file
    return 0;
}